# Sample Images

This folder contains the local reference images used to reproduce the paper-style visualizations.

The figure-generation script expects these files to exist:

- `dog.png`
- `airplane.png`
- `snake.png`

These images are treated as fixed local inputs. The script does not download or replace them automatically.
